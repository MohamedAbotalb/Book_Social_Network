spring:
  profiles:
    active: dev
    servlet:
      multipart:
        max-file-size: 10MB
  cache:
    type: caffeine
springdoc:
  default-produces-media-type: application/json
  swagger-ui:
    operations-sorter: alpha
    tags-sorter: alpha

server:
  servlet:
    context-path: /api/v1/